proc literals_11() int {
    return 1 + (3 + 4) + (5 - 6) - (4 * 5 * (-1)) / 5;
}

proc foo(a int, b int) int {
    return;
}

proc cond() int {

    #let foo int = 2;

    if 1 {
        return 45;
    } else {
        return 123;
    }
}
