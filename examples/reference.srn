# Inline comment

##
Multi-line comment
##

proc f() {} # Procedure

proc g(a 'int, b 'byte) 'size {} # Parameters and Returntype

proc h(x 'int) 'int; # Procedure Declaration

proc main() 'int { # Entry point

    # If Statement
    if 1 { ## Then ## } else { ## Else ## }

    # While Loop
    while 1 { ## Body ## }

    # Variable declaration
    let x 'int;

    # Variable definition
    let y 'int = 1;

    # Block
    { ## Body ## }

    return 0;
}
